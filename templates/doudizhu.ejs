<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700;800&display=swap');
        body {
            margin: 0;
            padding: 0;
            font-family: 'Noto Sans SC', 'Microsoft YaHei', sans-serif;
            display: inline-block;
            background: radial-gradient(circle at 20% 0%, #0f172a 0%, #020617 60%, #01030a 100%);
        }
        .felt {
            background:
                radial-gradient(circle at 20% 20%, rgba(255,255,255,0.06), transparent 35%),
                radial-gradient(circle at 80% 70%, rgba(255,255,255,0.05), transparent 30%),
                linear-gradient(145deg, #14532d 0%, #166534 45%, #0f3d24 100%);
            border: 3px solid #78350f;
            box-shadow: inset 0 2px 12px rgba(0,0,0,0.28), 0 18px 36px rgba(0,0,0,0.35);
        }
        .card {
            width: 34px;
            height: 48px;
            border-radius: 8px;
            background: linear-gradient(160deg, #ffffff 0%, #f8fafc 100%);
            border: 1px solid rgba(15,23,42,0.14);
            box-shadow: 0 8px 14px rgba(0,0,0,0.16);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 800;
            color: #0f172a;
            font-size: 14px;
        }
        .joker {
            background: linear-gradient(160deg, #fee2e2 0%, #fff7ed 100%);
            color: #b91c1c;
        }
        .chip {
            background: rgba(255,255,255,0.14);
            border: 1px solid rgba(255,255,255,0.22);
            border-radius: 10px;
            padding: 6px 10px;
        }
    </style>
</head>
<body>
    <%
    const ps = Array.isArray(players) ? players : [];
    const bottom = Array.isArray(bottom_cards) ? bottom_cards : [];
    function isJoker(r) { return r === 'SJ' || r === 'BJ'; }
    const width = 920;
    %>

    <div style="width: <%= width %>px;">
        <div class="rounded-t-2xl overflow-hidden">
            <div class="px-7 py-5 text-white" style="background: linear-gradient(90deg, #14532d 0%, #0f766e 45%, #f59e0b 100%);">
                <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                        <div class="text-3xl">🃟</div>
                        <div>
                            <div class="text-2xl font-bold">斗地主</div>
                            <div class="text-white/80 text-sm"><%= subtitle || '' %></div>
                        </div>
                    </div>
                    <div class="text-right">
                        <div class="chip text-sm font-semibold inline-block"><%= phase_text || '进行中' %></div>
                    </div>
                </div>
            </div>
        </div>

        <div class="bg-white px-6 py-5 rounded-b-2xl shadow-2xl">
            <div class="felt rounded-2xl p-5 text-white">
                <div class="flex justify-between items-center mb-4">
                    <div class="chip text-sm">地主: <span class="font-bold"><%= landlord_name || '未知' %></span></div>
                    <div class="chip text-sm">当前轮到: <span class="font-bold"><%= current_turn_name || '—' %></span></div>
                    <div class="chip text-sm">上手: <span class="font-bold"><%= last_play_text || '无' %></span></div>
                </div>

                <div class="mb-4">
                    <div class="text-xs text-white/70 mb-2">底牌</div>
                    <div class="flex gap-2">
                        <% bottom.forEach(function(r) { %>
                            <div class="card <%= isJoker(r) ? 'joker' : '' %>"><%= r %></div>
                        <% }); %>
                    </div>
                </div>

                <div class="space-y-3">
                    <% ps.forEach(function(p) { %>
                        <div class="rounded-xl px-3 py-3 <%= p.is_turn ? 'ring-2 ring-yellow-300 bg-white/15' : 'bg-white/10' %>">
                            <div class="flex justify-between items-start mb-2">
                                <div class="text-sm font-semibold"><%= p.name %> <span class="text-xs text-amber-200">(<%= p.role %>)</span></div>
                                <div class="text-xs text-white/80">剩余: <%= p.count %></div>
                            </div>
                            <div class="flex flex-wrap gap-1.5">
                                <% (p.cards || []).forEach(function(r) { %>
                                    <div class="card <%= isJoker(r) ? 'joker' : '' %>"><%= r %></div>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>

            <div class="mt-4 flex justify-between text-xs text-slate-500">
                <div class="truncate pr-3">最近动作: <%= last_action || '无' %></div>
                <% if (is_finished) { %>
                    <div class="font-semibold text-emerald-700"><%= winner_text || '游戏结束' %></div>
                <% } else { %>
                    <div>指令: /出牌斗地主 3,3,3,4 · /不出斗地主</div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
